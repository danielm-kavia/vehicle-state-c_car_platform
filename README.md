# vehicle-state-c_car_platform

Vehicle state service container (placeholder).

## Local development

> This container currently contains scaffolding only.

### Environment
Copy and edit:
- `.env.example` -> `.env`

### Commands (planned)
If/when a Node.js implementation is added:

```bash
npm install
npm run dev
npm run lint
npm run format
npm test
```
