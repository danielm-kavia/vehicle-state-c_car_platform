# Testing (Jest)

This service uses **Jest** for unit tests.

## Commands

From this service directory:

```bash
npm test
```

Watch mode:

```bash
npm run test:watch
```

Coverage:

```bash
npm run test:coverage
```

## Coverage output

Coverage artifacts are written to:

- `./coverage/lcov-report/index.html` (HTML report)
- `./coverage/lcov.info` (LCOV)
"""
